# Core/Content/Utility rollout (2026-02-14)

Source of truth: `config/minecraft_feature_core_content_util_2026-02-14.json` (core/content/utility grouped with component paths and configs).

## Core (sequential order)
1. **Hydrology-stable world generation** — share seam-safe hydrology/flow masks for caves, rivers, and lakes across server (`ImprovedTerrainCoordinator`, `ImprovedRiverGenerator`, `ImprovedLakeGenerator`, `ImprovedCaveGenerator`) and Unity (`WorldMapController`, `WorldMapControlProfile`).
2. **Map control profile sync** — hashed `world_map_control_profile.json` (now includes `lakeMaxRadius` + edge flow locks) generated by the server and mirrored to Unity StreamingAssets; regenerate via `dotnet run --project GameServer/GameServer.csproj -- --generate-map-profile`.
3. **Protobuf protocol validation** — enforce EnhancedMinecraft descriptor fingerprints at bootstrap (`ProtoRuntime.EnsureInitialized` + `ProtoFingerprint.AssertDescriptorFingerprint`) to catch stale generated DTOs on both runtimes.

## Content (sequential order)
1. **Riparian wetlands and delta bands** — hydrology-aware wetlands on river mouths and lake rims plus cave riparian plugs and delta sanding driven by the improved generation pipeline.

## Utility (sequential order)
1. **Config/profile sync** — JSON world + map-control configs stay in lockstep between `config/` and `Assets/StreamingAssets/` (chunk/sim distances, hydrology knobs, lake radius).
2. **Protobuf asset pipeline** — regenerate EnhancedMinecraft protobuf DTOs via `scripts/generate_proto.ps1` (or `protoc`) and validate with `SharedProtocol/SharedProtocol.csproj`.
