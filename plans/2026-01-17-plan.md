# 2026-01-17 Plan

## Completed (recent commits)
- cd67a223: docs – protocol and configuration audit.
- 44a5b19b: feat – hydrology envelope hardening and proto gating for worldgen.
- 78d9cadb: docs – Minecraft implementation comprehensive review.
- 19e52acb: chore – add hydrology envelope and signature guard.
- 24a66716: docs – implementation plan and status report.

## To Do (today)
- Refresh Minecraft feature list grouped into Core/Content/Util, align client/server needs, and store as file.
- Improve terrain generation algorithms for caves, rivers, lakes; integrate into world map control on server and client.
- Review protobuf-generated packets: references, usage, and regenerate if needed; fix handlers.
- Strengthen world map control architecture and code paths for synchronization between client/server.
- Ensure configs and data are data-driven JSON (server/client), add/update as needed.
- Verify `using` references resolve to existing classes/files; clean any broken imports.
- Update README or docs under `docs/` for changes (worldgen, proto, configs).
- Run builds/tests (SharedProtocol, GameServer, self-test if feasible); validate protobuf handling.
- Prepare commits and push to origin after completing work.

## Progress Notes
- Feature map refreshed and stored at `docs/minecraft_feature_core_content_util_2026-01-17.{md,json}`.
- Hydrology-aware terrain updates applied to server/client generators; README + docs updated.
- Protocol registry now rejects duplicate descriptor bindings after protoc regeneration.
- SharedProtocol/GameServer builds succeeded (warnings only); `dotnet run --project GameServer -- --selftest` completed with existing proto handler coverage warnings.
