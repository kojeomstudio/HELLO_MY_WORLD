syntax = "proto3";
package Game.Auth;
option csharp_namespace = "Game.Auth";

// Login request carries user credentials
message LoginRequest {
  string username = 1;
  string password = 2;
}

// Login response returns auth result
message LoginResponse {
  bool success = 1;
  string message = 2;
}
